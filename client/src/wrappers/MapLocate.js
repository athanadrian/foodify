import styled from 'styled-components';

const Wrapper = styled.section`
  .locate {
    position: absolute;
    top: 1.7rem;
    right: 1.2rem;
    background: none;
    border: none;
    z-index: 10;
  }
  .locate img {
    width: 30px;
    cursor: pointer;
  }
`;
export default Wrapper;
